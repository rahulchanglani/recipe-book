export class Ingredient {

  constructor(public name: string, public amount: number) {
    // Typescript fature - To assign class access type in args of constructor to assign and initialize local variables (properties) to constructor params
  }
}
